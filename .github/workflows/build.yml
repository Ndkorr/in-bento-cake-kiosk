name: Build and Deploy Flutter App

on:
  push:
    branches: [ "main" ]
    tags: [ 'v*.*.*' ]

jobs:
  build-and-deploy-web:
    if: github.ref == 'refs/heads/main'
    name: Build and Deploy Web
    permissions:
      contents: write
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Detect Flutter project directory
        shell: bash
        run: |
          if [ -f pubspec.yaml ]; then
            echo "PROJECT_DIR=." >> $GITHUB_ENV
            echo "Detected Flutter project at repo root."
          elif [ -f in_bento_kiosk/pubspec.yaml ]; then
            echo "PROJECT_DIR=in_bento_kiosk" >> $GITHUB_ENV
            echo "Detected Flutter project in ./in_bento_kiosk"
          else
            echo "pubspec.yaml not found in repo root or ./in_bento_kiosk" >&2
            exit 1
          fi

      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: 'stable'
          flutter-version: '3.24.3'

      - name: Get Flutter dependencies
        working-directory: ${{ env.PROJECT_DIR }}
        run: flutter pub get

      - name: Verify GLB files exist
        shell: bash
        run: |
          echo "Checking for GLB files..."
          find "$PROJECT_DIR/assets/cake_layers" -name "*.glb" -type f
          echo "Total GLB files found:"
          find "$PROJECT_DIR/assets/cake_layers" -name "*.glb" -type f | wc -l

      - name: Build Web (CanvasKit, no PWA caching)
        working-directory: ${{ env.PROJECT_DIR }}
        run: flutter build web --release --base-href /in-bento-cake-kiosk/ --web-renderer canvaskit --pwa-strategy=none

      - name: Verify assets in build output
        shell: bash
        run: |
          echo "Checking if GLB files are in build output..."
          if [ ! -d "$PROJECT_DIR/build/web/assets" ]; then
            echo "Assets directory not found!"; exit 1; fi
          find "$PROJECT_DIR/build/web/assets/cake_layers" -name "*.glb" -type f | sed 's/^/  -> /'
          COUNT=$(find "$PROJECT_DIR/build/web/assets/cake_layers" -name "*.glb" -type f | wc -l)
          echo "Total GLB files in build: $COUNT"
          if [ "$COUNT" -eq 0 ]; then echo "No GLB files found in build!"; exit 1; fi
          # Explicitly verify the chocolate.glb path reported by the 404
          if [ ! -f "$PROJECT_DIR/build/web/assets/cake_layers/full_view/heartshaped/2layers/chocolate.glb" ]; then
            echo "Missing: build/web/assets/cake_layers/full_view/heartshaped/2layers/chocolate.glb"; exit 1; fi

      - name: Upload web build as artifact (for inspection)
        uses: actions/upload-artifact@v4
        with:
          name: web-build
          path: ${{ env.PROJECT_DIR }}/build/web

      - name: Create .nojekyll file
        run: touch ${{ env.PROJECT_DIR }}/build/web/.nojekyll

      - name: Deploy to GitHub Pages
        uses: peaceiris/actions-gh-pages@v4
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./${{ env.PROJECT_DIR }}/build/web

  create-release:
    if: startsWith(github.ref, 'refs/tags/')
    name: Create Release and Upload APK
    permissions:
      contents: write
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Detect Flutter project directory
        shell: bash
        run: |
          if [ -f pubspec.yaml ]; then
            echo "PROJECT_DIR=." >> $GITHUB_ENV
          elif [ -f in_bento_kiosk/pubspec.yaml ]; then
            echo "PROJECT_DIR=in_bento_kiosk" >> $GITHUB_ENV
          else
            echo "pubspec.yaml not found" >&2
            exit 1
          fi

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: 'stable'
          flutter-version: '3.24.3'

      - name: Get Flutter dependencies
        working-directory: ${{ env.PROJECT_DIR }}
        run: flutter pub get

      - name: Build APK
        working-directory: ${{ env.PROJECT_DIR }}
        run: flutter build apk --release

      - name: Extract version from pubspec.yaml
        id: get_version
        working-directory: ${{ env.PROJECT_DIR }}
        run: | 
          echo "version=$(grep 'version: ' pubspec.yaml | sed 's/version: //')" >> $GITHUB_OUTPUT

      - name: Create Release and Upload APK
        uses: softprops/action-gh-release@v2
        with:
          name: Release ${{ steps.get_version.outputs.version }}
          files: ${{ env.PROJECT_DIR }}/build/app/outputs/flutter-apk/app-release.apk